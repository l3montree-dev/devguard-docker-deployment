# Interactive first-time configuration.
# Runs the Bun setup script in a throwaway container.
#
#   docker compose -f compose.configure.yaml run --rm configure
#
# It writes .env to the repo (via the bind mount)
services:
  configure:
    image: oven/bun:1.3.14@sha256:e10577f0db68676a7024391c6e5cb4b879ebd17188ab750cf10024a6d700e5c4
    working_dir: /app
    volumes:
      - ./:/app
      - encryption-key:/keys
      - kratos-config:/kratos
      - initdb:/initdb
    command: ["bun", "run", "dev/setup.ts"]

volumes:
  encryption-key:
  kratos-config:
  initdb:
